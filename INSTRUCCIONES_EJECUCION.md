# 🚀 Instrucciones para Ejecutar el Proyecto CMS Multipaís

## ✅ Estado del Proyecto

**BACKEND:** ✅ Completamente configurado
**FRONTEND:** ✅ Completamente configurado

---

## 📋 PASO 1: Configurar Base de Datos en Supabase

### 1.1 Crear las tablas en Supabase

Ve a tu proyecto en Supabase → **SQL Editor** y ejecuta los siguientes scripts **EN ORDEN**:

#### A. Crear tablas base

```sql
-- Tabla paises
create table if not exists paises (
  id bigint generated by default as identity primary key,
  nombre text not null,
  codigo text not null unique,
  slug text not null unique,
  estado text not null default 'activo',
  created_at timestamptz default now(),
  updated_at timestamptz default now()
);

-- Tabla roles
create table if not exists roles (
  id bigint generated by default as identity primary key,
  nombre text not null unique,
  descripcion text,
  created_at timestamptz default now()
);

-- Tabla usuarios
create table if not exists usuarios (
  id bigint generated by default as identity primary key,
  nombre text not null,
  apellido text not null,
  email text not null unique,
  username text not null unique,
  password_hash text not null,
  rol_id bigint not null references roles(id),
  pais_id bigint references paises(id),
  estado text not null default 'activo',
  ultimo_acceso timestamptz,
  created_at timestamptz default now(),
  updated_at timestamptz default now()
);
```

#### B. Agregar campos de seguridad a usuarios

```sql
alter table usuarios
add column if not exists pregunta_seguridad text,
add column if not exists respuesta_seguridad_hash text,
add column if not exists login_intentos int default 0,
add column if not exists login_bloqueado_hasta timestamptz;
```

#### C. Insertar datos iniciales

```sql
-- Insertar países
insert into paises (nombre, codigo, slug, estado)
values
('Argentina', 'AR', 'argentina', 'activo'),
('Chile', 'CL', 'chile', 'activo'),
('Ecuador', 'EC', 'ecuador', 'activo')
on conflict (codigo) do nothing;

-- Insertar roles
insert into roles (nombre, descripcion)
values
('superadmin', 'Administra todo el sistema y todos los países'),
('admin_pais', 'Administra únicamente el portal del país asignado'),
('editor', 'Crea y edita contenido, pero no administra usuarios')
on conflict (nombre) do nothing;
```

#### D. Crear tabla noticias

```sql
create table if not exists noticias (
  id bigint generated by default as identity primary key,
  pais_id bigint not null references paises(id),
  titulo text not null,
  slug text not null,
  resumen text not null,
  contenido text not null,
  imagen_principal_url text,
  autor_id bigint not null references usuarios(id),
  estado text not null default 'borrador',
  fecha_publicacion timestamptz,
  created_at timestamptz default now(),
  updated_at timestamptz default now(),
  constraint noticias_estado_check
  check (estado in ('borrador', 'publicado', 'despublicado')),
  constraint noticias_slug_pais_unique
  unique (pais_id, slug)
);
```

#### E. Crear tabla testimonios

```sql
create table if not exists testimonios (
  id bigint generated by default as identity primary key,
  pais_id bigint not null references paises(id),
  nombre text not null,
  cargo text,
  empresa text,
  contenido text not null,
  foto_url text not null,
  instagram_url text,
  facebook_url text,
  estado text not null default 'borrador',
  destacado boolean not null default false,
  autor_id bigint not null references usuarios(id),
  fecha_publicacion timestamptz,
  created_at timestamptz default now(),
  updated_at timestamptz default now(),
  constraint testimonios_estado_check
  check (estado in ('borrador', 'publicado', 'despublicado'))
);
```

#### F. Crear tabla solicitudes_contacto

```sql
create table if not exists solicitudes_contacto (
  id bigint generated by default as identity primary key,
  pais_id bigint not null references paises(id),
  nombre text not null,
  correo text not null,
  telefono text not null,
  finalidad text not null,
  mensaje text,
  estado text not null default 'pendiente',
  observaciones_admin text,
  fecha_gestion timestamptz,
  gestionado_por bigint references usuarios(id),
  created_at timestamptz default now(),
  updated_at timestamptz default now(),
  constraint solicitudes_estado_check
  check (estado in ('pendiente', 'en_proceso', 'gestionada', 'cerrada')),
  constraint solicitudes_finalidad_check
  check (finalidad in ('Servicio', 'Programa EDIFICA', 'Shows y conferencias'))
);
```

#### G. Crear tabla bitacora_auditoria

```sql
create table if not exists bitacora_auditoria (
  id bigint generated by default as identity primary key,
  usuario_id bigint references usuarios(id),
  accion text not null,
  modulo text not null,
  registro_id bigint,
  descripcion text,
  ip text,
  created_at timestamptz default now()
);
```

#### H. Crear tabla archivos

```sql
create table if not exists archivos (
  id bigint generated by default as identity primary key,
  nombre_archivo text not null,
  url text not null,
  tipo_archivo text not null,
  modulo text not null,
  referencia_id bigint,
  bucket text default 'cms-media',
  storage_path text,
  subido_por bigint references usuarios(id),
  created_at timestamptz default now(),
  updated_at timestamptz default now(),
  constraint archivos_modulo_check
  check (modulo in ('noticias', 'testimonios', 'solicitudes_contacto', 'general'))
);
```

#### I. Activar RLS (Row Level Security)

```sql
alter table public.paises enable row level security;
alter table public.roles enable row level security;
alter table public.usuarios enable row level security;
alter table public.noticias enable row level security;
alter table public.testimonios enable row level security;
alter table public.solicitudes_contacto enable row level security;
alter table public.bitacora_auditoria enable row level security;
alter table public.archivos enable row level security;
```

### 1.2 Crear bucket de Storage (Opcional - para subir imágenes)

1. Ve a **Storage** en Supabase
2. Click en **New bucket**
3. Nombre: `cms-media`
4. Marca como **Public bucket**
5. Click en **Create bucket**

---

## 📋 PASO 2: Instalar Dependencias

### 2.1 Backend

```bash
cd backend
npm install
```

### 2.2 Frontend

```bash
cd frontend
npm install
```

---

## 📋 PASO 3: Crear Usuario SuperAdmin

Desde la carpeta `backend`, ejecuta:

```bash
cd backend
npm run create-superadmin
```

**Credenciales del SuperAdmin:**
- Usuario: `superadmin`
- Contraseña: `superadmin123*`

---

## 📋 PASO 4: Crear Usuarios de Prueba (Opcional)

```bash
cd backend
npm run create-test-users
```

Esto creará:
- **admin_argentina** / `admin123*`
- **editor_argentina** / `editor123*`

---

## 📋 PASO 5: Ejecutar el Proyecto

### 5.1 Iniciar Backend

Abre una terminal en `backend`:

```bash
cd backend
npm run dev
```

El backend estará corriendo en: **http://localhost:3001**

### 5.2 Iniciar Frontend

Abre OTRA terminal en `frontend`:

```bash
cd frontend
npm run dev
```

El frontend estará corriendo en: **http://localhost:5173**

---

## 🎯 PASO 6: Probar el Sistema

### Portal Público

1. Abre: **http://localhost:5173**
2. Verás la página de inicio con los países
3. Navega a noticias, testimonios o solicitudes

### Panel Administrativo

1. Ve a: **http://localhost:5173/admin/login**
2. Ingresa con:
   - Usuario: `superadmin`
   - Contraseña: `superadmin123*`
3. Explora el dashboard y las diferentes secciones

---

## 📝 Funcionalidades Disponibles

### Como SuperAdmin puedes:
- ✅ Gestionar usuarios
- ✅ Ver auditoría
- ✅ Crear/editar/eliminar noticias de todos los países
- ✅ Crear/editar/eliminar testimonios de todos los países
- ✅ Gestionar solicitudes de contacto
- ✅ Actualizar tu perfil
- ✅ Cambiar contraseña
- ✅ Configurar pregunta de seguridad

### Como Admin País puedes:
- ✅ Crear/editar/eliminar noticias de tu país
- ✅ Crear/editar/eliminar testimonios de tu país
- ✅ Gestionar solicitudes de tu país
- ✅ Actualizar tu perfil

### Como Editor puedes:
- ✅ Crear/editar noticias de tu país (no eliminar)
- ✅ Crear/editar testimonios de tu país (no eliminar)
- ✅ Actualizar tu perfil

---

## 🔧 Solución de Problemas

### Error de conexión al backend

Verifica que:
1. El backend esté corriendo en el puerto 3001
2. El archivo `frontend/.env` tenga: `VITE_API_URL=http://localhost:3001/api`

### Error de Supabase

Verifica que:
1. Las credenciales en `backend/.env` sean correctas
2. Todas las tablas estén creadas
3. RLS esté activado

### Error al crear superadmin

Si ya existe, puedes:
1. Ir a Supabase → Table Editor → usuarios
2. Eliminar el usuario existente
3. Volver a ejecutar desde la carpeta backend: `npm run create-superadmin`

---

## 📚 Rutas del API Backend

### Autenticación
- `POST /api/auth/login` - Iniciar sesión
- `PATCH /api/auth/me` - Actualizar mi perfil
- `PATCH /api/auth/change-my-password` - Cambiar mi contraseña
- `PATCH /api/auth/security-question` - Configurar pregunta de seguridad
- `POST /api/auth/security-question` - Consultar pregunta de seguridad
- `POST /api/auth/forgot-password` - Recuperar contraseña

### Usuarios (Solo SuperAdmin)
- `GET /api/users` - Listar usuarios
- `GET /api/users/:id` - Ver usuario
- `POST /api/users` - Crear usuario
- `PUT /api/users/:id` - Actualizar usuario completo
- `PATCH /api/users/:id` - Actualizar usuario parcial
- `DELETE /api/users/:id` - Desactivar usuario
- `DELETE /api/users/:id/permanent` - Eliminar usuario definitivamente

### Noticias
- `GET /api/noticias` - Listar noticias
- `GET /api/noticias/:id` - Ver noticia
- `POST /api/noticias` - Crear noticia
- `PUT /api/noticias/:id` - Actualizar noticia completa
- `PATCH /api/noticias/:id` - Actualizar noticia parcial
- `PATCH /api/noticias/:id/estado` - Cambiar estado
- `DELETE /api/noticias/:id` - Eliminar noticia

### Testimonios
- `GET /api/testimonios` - Listar testimonios
- `GET /api/testimonios/:id` - Ver testimonio
- `POST /api/testimonios` - Crear testimonio
- `PUT /api/testimonios/:id` - Actualizar testimonio completo
- `PATCH /api/testimonios/:id` - Actualizar testimonio parcial
- `PATCH /api/testimonios/:id/estado` - Cambiar estado
- `DELETE /api/testimonios/:id` - Eliminar testimonio

### Solicitudes
- `POST /api/solicitudes/public` - Crear solicitud pública (sin token)
- `GET /api/solicitudes` - Listar solicitudes
- `GET /api/solicitudes/:id` - Ver solicitud
- `PUT /api/solicitudes/:id` - Actualizar solicitud completa
- `PATCH /api/solicitudes/:id` - Actualizar solicitud parcial
- `PATCH /api/solicitudes/:id/estado` - Cambiar estado
- `DELETE /api/solicitudes/:id` - Eliminar solicitud

### Auditoría (Solo SuperAdmin)
- `GET /api/auditoria` - Listar auditoría
- `GET /api/auditoria/:id` - Ver detalle de auditoría

### Rutas Públicas (sin autenticación)
- `GET /api/public/paises/:paisSlug/noticias` - Noticias publicadas por país
- `GET /api/public/paises/:paisSlug/noticias/:noticiaSlug` - Detalle de noticia
- `GET /api/public/paises/:paisSlug/testimonios` - Testimonios publicados por país
- `GET /api/public/paises/:paisSlug/testimonios/:id` - Detalle de testimonio

---

## ✅ Checklist de Verificación

- [ ] Backend corriendo en puerto 3001
- [ ] Frontend corriendo en puerto 5173
- [ ] Todas las tablas creadas en Supabase
- [ ] SuperAdmin creado
- [ ] Puedo hacer login
- [ ] Puedo ver el dashboard
- [ ] Puedo crear una noticia
- [ ] Puedo ver noticias en el portal público
- [ ] Puedo enviar una solicitud desde el portal público

---

## 🎉 ¡Listo!

Tu proyecto CMS Multipaís está completamente configurado y funcionando.

**Próximos pasos sugeridos:**
1. Crear contenido de prueba (noticias, testimonios)
2. Probar el flujo de solicitudes públicas
3. Configurar el bucket de Storage para subir imágenes reales
4. Personalizar estilos según tu marca

---

## 📞 Soporte

Si encuentras algún problema:
1. Verifica que todas las tablas estén creadas
2. Revisa la consola del navegador para errores
3. Revisa la terminal del backend para errores
4. Verifica que las credenciales de Supabase sean correctas
